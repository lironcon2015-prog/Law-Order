# PROJECT_KNOWLEDGE — LexLedger

מסמך ידע חי. עדכן בסוף שיחה משמעותית. גרסה נוכחית: **v7** (root) / **unified-v15** (v2) לפי `CACHE` ב-`sw.js`.

## 1. מצב נוכחי
CRM אישי לעו"ד M&A, vanilla JS · PWA · offline-first · RTL. חי ב-`lironcon.com/crm` (דרך Cloudflare Worker מעל GitHub Pages/main).
פיצ'רים פעילים:
- **שתי תצוגות** (`state.mainView`): **Pipeline** (קנבן לפי שלבי משפך, ברירת מחדל) ו-**List** (sidebar + detail). מתג בכותרת.
- **Pipeline**: נתיב צבעוני לשלב, כרטיסים צפים (תמונה/ראשי-תיבות, badge דחיפות, שווי עסקה מצטבר, אינטראקציה אחרונה), drag&drop בין נתיבים = שינוי שלב.
- **תצוגת פרטים בטאבים** (סקירה/קריירה/הפניות/הערות) — ב-drawer צד (Pipeline) או ב-`#main` (List).
- **תמונות פרופיל**: העלאת קובץ (מוקטן ל-256px, dataURL ב-IndexedDB, offline) או URL; נפילה לראשי-תיבות.
- **סוג איש קשר** (`contactType`): בעל תפקיד בחברה / מתווך / בנקאי השקעות / משקיע / עו"ד / אחר. חברה אופציונלית.
- **מנהל חברות**: רשימה + מחיקה (עם אזהרת אנשי קשר מושפעים) + הוספה.
- **סנכרון Google Drive** (`js/sync.js`): קובץ JSON יחיד, OAuth client-side, auto-pull בטעינה, push אוטומטי מבוזבז, דיאלוג קונפליקט. `CLIENT_ID` מוזן.
- **v2 (מאוחד, `v2/`)**: ייבוא חשבוניות מקובץ **PDF/Excel/CSV** עם זיהוי וסיווג אוטומטי (`v2/js/invoice-import.js` + vendor מקומי SheetJS/pdf.js ב-precache → offline). כפתור במסך חשבוניות → מסך סיווג לאישור.

## 2. מודל נתונים (IndexedDB `maCrmDB`)
- **contacts**: `id, fullName, status (LEAD_STATUSES), contactType, currentCompanyId, role, origin, photoUrl, contactInfo{phone,email,linkedin}, tags[], careerTimeline[{companyId,companyName,role,startYear,endYear}], referrals[{dealName,status,estimatedValue}], chronologicalNotes[{timestamp,noteText}], lastContactDate, contactFrequencyDays`.
- **companies**: `id, name, sector, investors[], website`.
- **משפך** `LEAD_STATUSES`: cold→warm→talking→meeting→proposal→client (+frozen). tier קובע צבע/דרגה.
- **שווי הפניה = שווי שנתי** (`estimatedValue`). `dealValue` = סכום ההפניות.

## 3. ארכיטקטורה / DNA
- **state יחיד** ב-`app.js`; `render()` הוא מקור האמת, מנותב לפי `mainView`. UI-state (selection/tab/search) בזיכרון, לא נשמר.
- **ניתוב פרטים**: `detailTarget()` → drawer (pipeline) / `#main` (list). מעבר חלק עם View Transitions.
- **DOM ב-`ui.js`** עם helper `el()`; כל קלט משתמש כ-`textContent` (XSS). אייקונים SVG סטטיים.
- **סנכרון**: `collectBackupData`=stores→JSON, `applyBackupData`=`db.replaceAll` (clear+put), `_suppressPush` בזמן apply, `isValidShape` guard לפני החלפה. token בזיכרון בלבד.
- **SW**: app-shell network-first (פרסום מיידי), fonts/icons cache-first, auto-reload ב-controllerchange.

## 4. החלטות עיצוב/לוגיקה
- כיוון עיצובי: **Obsidian Gold** (זהב מותג) + רוח **Stitch** ל-Pipeline (צבע לכל שלב; `client`=זהב — כלאיים, לא ניאון מלא).
- **שכבת Luxury (v3+v4 ב-styles.css)**: serif עברי **Frank Ruhl Libre** (מקומי, `fonts/frank-ruhl-*.woff2`) לכותרות/שמות/מספרי display; hairlines בגרדיאנט על משטחי זכוכית (mask-composite); sheen ללוגו ולכפתור ראשי; stagger בכניסת כרטיסים; טבעת conic מסתובבת ל-tier לקוח; View Transitions בין מסכים. **קיים גם ב-`v2/`** (סלקטורים מותאמים: `topbar`/`sb-logo`/`sb-brand-name`). כל לולאה אינסופית מכובה ב-`prefers-reduced-motion`.
- **`LexLedger-Offline.html` נבנה מ-`v2/` בלבד** (`node v2/build-offline.mjs`) — שינוי עיצוב/JS ב-v2 מחייב rebuild של הקובץ + commit שלו.
- מסלול קריירה ממוין **כרונולוגי עולה** (מוקדם→מאוחר; חסרי-שנה לסוף).
- אירוח על **נתיב** `/crm` חייב Cloudflare Worker — GitHub Pages לא מארח על path (רק host שלם).

## 5. חוב טכני / סיכונים
- מחיקת חברה משאירה `currentCompanyId` "תלוי" אצל אנשי קשר (מוצג ריק; לא נוקה ביודעין — בטוח).
- אין merge חכם בסנכרון — last-writer-wins + שאלת המשתמש בלבד (מכוון).
- בדיקת OAuth/Drive end-to-end רק אצל המשתמש (אין Google בסביבת הפיתוח).
- seed (`seedDemo`) לא כולל `contactType`/`photoUrl` — נתוני דמו ישנים יציגו ברירות מחדל.

## 5.1 פתיחת לקוח מתוך מייל (v2)
- `v2/js/email-parse.js` — `parseClientEmail(text)` מחזיר טיוטה + `missing[]` + `inferred[]`. **לא משלים שדות חסרים.**
  הרחבת זיהוי = הוספת תווית ל-`LABELS` (סדר = עדיפות) או כלל ל-`CASE_TYPE_RULES`. בדיקות: `node tools/test-email-parse.mjs`.
- מודל הלקוח (`clients`) הורחב: `clientNumber`, `contactName`, `contactEmail`, `contactPhone`, `crmContactId`.
  `clients.add` מקבל מחרוזת (תאימות לאחור) או אובייקט.
- `cases.add` שומר `caseType: ''` כשנמסר ריק (ברירת מחדל 'שוטף' רק כשהשדה כלל לא נמסר) — כדי לא להציג סוג שלא נאמר.
- **HTML אופליין**: כל מודול חדש ב-`v2/js/` חייב להיכנס למערך `ORDER` ב-`v2/build-offline.mjs` (סדר תלויות),
  אחרת הוא לא ייארז. ה-IndexedDB של `file://` מבודד מזה של האתר — ההעברה בין השניים היא ייבוא/ייצוא JSON במסך ההגדרות.

## 5.2 מסמכי מקור והשלמה מחשבונית (v2)
- **DB v3**: `invoiceFiles` (מסמכי המקור) + `localPrefs` (handle לתיקייה). שניהם **מקומיים** — לא ב-`collectBackupData`
  ולא ב-`importer.collectAll`. מכשיר אחר יראה את החשבוניות אבל לא יוכל לפתוח את הקבצים (מוצג "הקובץ אינו זמין").
- `invoices.fileId` = מזהה מסמך המקור; `null` = הוזן ידנית. `file-store.js` מטפל בשני מצבי אחסון ('folder'/'db').
- File System Access זמין גם ב-`file://` בכרום (נבדק) — לכן קובץ האופליין יכול לכתוב לתיקייה אמיתית.
  ההרשאה מתחדשת רק מתוך לחיצת משתמש (`requestPermission`).
- **השלמה = שדות ריקים בלבד**, לעולם לא דריסה (`backfillFromInvoice`). בחירת תיק במסך הסיווג לא מאפסת אחוז
  עמלה שזוהה בחשבונית (התיק גובר רק אם יש לו אחוז).
- **SheetJS ו-CSV**: `cellDates` הפך `12/03/2026` ל-3 בדצמבר ואת `4471/1` לתאריך. CSV מפוענח עכשיו ב-`parseCsv`
  שלנו (כל תא נשאר טקסט); ל-Excel בינארי מועבר `dateNF: 'dd/mm/yyyy'`.

## 5א. LexBudget — מערכת תקציב עסקה (`/budget/`, נפרדת לחלוטין)
מערכת שנייה בריפו, ללא תלות ב-CRM: DB משלה (`lexBudgetDB`), SW משלה (`lexbudget-v1`, scope `/budget/`), manifest משלה.
משותפת רק **השפה העיצובית** (טוקנים של Obsidian Gold מועתקים ל-`budget/css/styles.css`) והפונטים.
- **מודל**: `deals` → `teams` (שורה לכל דרגה) → `entries` (ביצוע: שעות/חשבונות/הוצאות) + `files` (Blob של חשבון) + `rateCards`.
- **מתודולוגיה** (מגיליון המשתמש): שעות תקציב = `ROUNDUP(מוערך × (1+מקדם))`; עלות = שעות×תעריף; בלנדד = ללא דרגות ג'וניור.
- **מנוע החישוב** ב-`model.js` (`computeDeal`) — טהור, ללא DOM/DB. `app.js` מחזיק state יחיד ו-`render()`.
- **עריכה חיה בגיליון**: `input` מעדכן את האובייקט בזיכרון + `ui.refreshComputed()` (עדכון `[data-calc]` בלבד, שמירת פוקוס); `change` מפעיל `store.saveTeam`.
- **קריאת XLSX ללא ספריות** (`xlsx.js`): פענוח ZIP ידני + `DecompressionStream('deflate-raw')`, sharedStrings, זיהוי תאי תאריך מ-`styles.xml`.
- **ייבוא תקציב** (`parseBudgetSheet`): העמודה עם הכי הרבה שמות דרגה = עמודת התקציב; דרגות בעמודות אחרות = טבלת תעריפים; מספרים נלקחים רק משמאל לעמודת התעריפים.
- **CSV בעברית**: גרשיים נחשבים פותחי-ציטוט רק בתחילת שדה — אחרת `עו"ד`/`רו"ח` שוברים את הפרסור.
- מה שאין (מכוון): סנכרון Drive, קישור ל-CRM. גיבוי = ייצוא/ייבוא JSON מקומי.
- **דרגות מול תעריפונים**: `roleId` ייחודי לכל תעריפון, ולכן שורת תקציב שומרת גם `roleName`.
  החלפת תעריפון בעסקה (או מחיקת תעריפון) מריצה `store.remapDealToRateCard` — התאמה **לפי שם**,
  ולשורות ללא מקבילה התעריף "מוקפא" כ-`rateOverride` (במקום ₪0 שקט). נתונים ישנים ללא `roleName`
  מרופאים בטעינה (`healOrphanLines`, התאמה לפי סדר כששורות הצוות כולן יתומות ומספרן = מספר הדרגות).
- **בלנדד** = `seniorCost / seniorHours` (הג'וניורים יוצאים משני צדי השבר) — כך במתודולוגיה של המשתמש.
  *(נוסתה גרסת `budgetCost/seniorHours` והוחזרה לבקשתו.)*
- **DB v2**: store חדש `progress` — כל עדכון ביצוע במעקב השוטף (`hours` = תוספת לתקופה, גם שלילית).
  הסך המצטבר של שורה = סכום הרשומות; הזנה ישירה בגיליון נרשמת כ**דלתא** (`setLineManualTotal`).
  `line.manualHours` הישן מהוגר לרשומה מתוארכת בטעינה (`migrateManualHours`).
  ⚠️ ב-`computeDeal` הפרמטר הוא `progress` אבל השדה בסנאפשוט הוא `progressLog` — `snap.progress` הוא
  אחוז ההתקדמות המדווח (התנגשות שמות שכבר שברה את המסך פעם אחת).
- **ייבוא דוח שעות**: `rowsToProgress` עם `cumulative` — בדוח מצטבר נרשם רק ההפרש מול מה שכבר דווח.
  השיוך הוא לאדם→**שורת תקציב** (לא רק צוות), עם שיוך מהיר לכל הצוות ולפי הדרגה שבדוח.
  **זיהוי חפיפה**: `progressKey` = `lineId|date|person` מול הדיווחים הקיימים. ברירת מחדל — דילוג;
  `replace` מוחק דרך `clearImportedProgressRange` רק רשומות `source==='import'` בטווח (ידני מוגן).
- **מדד יחיד — שעות** (עדכון מהותי): `reconcileExecution` מאחד `progress` + `entries` (שעות בלבד)
  לרשימה אחת; דירוג מקורות `EXEC_SOURCES` (ידני 1 < דוח 2 < חשבון 3); רשומה ממקור גבוה **מחליפה**
  רשומות נמוכות יותר שתאריכן בתוך `periodFrom..periodTo` שלה (לאותה שורה ואותו אדם).
  עלות בכל מקום = שעות × תעריף התכנון. סכום החשבון, מע"מ וסטטוס החיוב אינם חלק מהחישוב.
  *(בוטל: המודל הקודם של)* שני מסלולים נפרדים — המעקב השוטף מול רישומי ה-`entries`
  (חשבונות שיובאו). המנוע מחזיר לכל שורה/צוות/עסקה שתי קבוצות שדות — `manual*` מול הרגילות —
  ו**אין שום מקום שסוכם ביניהן**. כותרת העסקה/הטאב/הכרטיס מציגות את המסלול הידני כשהוזן (`headlineUtil`).
- **תקרת שכ"ט**: `capFactor = תקרה ÷ עלות` (כשהעלות גדולה מהתקרה) מוחל על כל תעריף ועל הממוצע הכולל
  (`snap.effectiveRates`). מחושב שלוש פעמים — מול התקציב, מול החשבונות ומול המעקב הידני.
  ⚠️ **הבלנדד ללא ג'וניור הוא היוצא מן הכלל**: `(מה שנגבה − עלות הג'וניורים) ÷ שעות הבכירים`,
  ולא `blendedRate × capFactor`. הנוסחה הישנה השאירה את עלות הג'וניורים בתוך המקדם (תקרה ÷ תקציב
  *כולל* ג'וניורים) ולכן גלגלה עליהם חלק מההנחה — הפוך מהמתודולוגיה שבה הג'וניורים יוצאים משני צדי
  השבר. אותו תיקון ב-`realized.blendedSenior`, ששם התסמין היה בולט: השכ"ט *כולו* חולק בשעות הבכירים
  והתקבל מספר גבוה מכל תעריף בתעריפון (₪910 מול תעריף שותף ₪900). מבחן שפיות: כשאין תקרה שתי
  הנוסחאות חוזרות בדיוק לבלנדד המתוכנן.
- פאנל התקרה נבנה מחדש ב-`refreshComputed` (אין בו קלט) — תאי `data-calc` לא הספיקו לו.
- `charts.barCompare` הוא **HTML ולא SVG**: טקסט עברי ב-SVG עם `direction:rtl` ו-`text-anchor:end` גלש
  מחוץ ל-viewBox ונראתה ממנו אות אחת בלבד.
- **רענון חי**: `ui.refreshComputed` מעדכן גם את סרגל הכותרת, יתרה/ניצול לשורה ולצוות, הסיכום המצרפי
  ואת אחוז הניצול בטאב. כל תא מחושב חדש חייב `data-calc` + שורה ב-`refreshComputed`, אחרת יישאר תקוע.
- **פוקוס בהקלדה**: החלפת DOM בתוך אירוע `input` מאבדת פוקוס (התו הראשון בלבד נקלט). לכן טבלת הביצוע
  יושבת ב-`.actuals-body` נפרד ו-`renderActualsList` מרנדר רק אותה — סרגל הסינון לא נהרס.
- **שיוך אנשים לצוותים**: `settings.peopleTeams` = `{ שם מנורמל: { name, teamName, roleName, splits[] } }`.
  נשמר לפי **שם הצוות** כדי לחול על עסקאות עתידיות; `store.peopleTeamIdsFor(dealId)` מתרגם ל-teamId
  של העסקה הנוכחית, ו-`store.peopleAllocFor(dealId)` מתרגם את `splits` (אלוקציה רב-צוותית) לאותה עסקה.
- **אלוקציית שעות בין צוותים** (חבר צוות שמשתייך לכמה צוותים):
  - הדיווח הוא **מקור אחד שמתפצל**: כל רשומה ב-`progress` נושאת `allocGroupId` (מקשר את הרשומות
    שנוצרו מאותה שורת דוח), `allocPct` ו-`sourceHours` (הסכום לפני החלוקה).
  - `model.allocateHours(total, pcts)` מחלק לפי אחוזים עם **largest-remainder** על אגורות — הסכום
    המוחזר שווה בדיוק לקלט (אין שעות שנעלמות או נולדות בעיגול).
  - `importer.rowsToProgress` מקבל מ-`resolve` **מערך יעדים** ([{teamId,lineId,pct}]) ולא יעד יחיד.
  - **תיקון בדיעבד**: `store.reallocatePerson({dealId, key, splits, scope})` מקבץ את דיווחי האדם לפי
    `allocGroupId`, מחשב מחדש לפי האחוזים החדשים ומחליף את הרשומות. `scope` = כל העסקה /
    `period:YYYY-MM` / `batch:<id>` — כך אפשר לתקן רק תקופה או רק דוח מסוים. הסך הכולל נשמר.
  - האלוקציה הפעילה נשמרת ב-`settings.allocations[dealId][personKey]` ונטענת בייבוא הבא.
  - **הקמת איש צוות מהייבוא**: `store.addTeamMember(teamId, {person, roleId, rate})` פותח שורת תקציב
    חדשה על שמו ומשייך אליה מיד; המודאל נפתח מעל מודאל הייבוא ומחזיר אליו (‏`importCtx` לא נמחק —
    אסור לקרוא ל-`closeModal` בדרך, היא מאפסת אותו).
- **ייבוא תקציב**: טבלת תעריפים באותה עמודה של שורות התקציב זוהתה כ"צוות 1" עם תעריפים כשעות.
  כעת שורת דרגה עם מספר יחיד לפני הצוות הראשון (או תחת כותרת "תעריפים") נקראת כתעריף.
- **סדר צוותים**: `nextTeamOrder` = max+1 (לא `length`) — אחרת אחרי מחיקה נוצרות התנגשויות סדר.
- **קובץ אופליין**: `budget/build-offline.mjs` → `LexBudget-Offline.html` בשורש (זהה במנגנון ל-v2: מודולים
  כ-base64 → blob URLs, CSS/פונטים מוטבעים). מודול חדש ב-`budget/js/` חייב להיכנס ל-`ORDER` בסקריפט.
  ה-IndexedDB של `file://` מבודד מזה של האתר — מעבר בין השניים רק דרך ייצוא/ייבוא JSON (כפתור "גיבוי").

### 5א.1 חוב טכני — LexBudget
*(בוצעו בסבב האחרון: סיווג צוות בדיקת נאותות + הפרדה בכל החיתוכים ובאקסלים · כפתור ייצוא בראש טאב התקציב · עיצוב שורות סיכום באקסל · "% מהצוות" לכל חבר צוות.)*
- **אי אפשר לסגור התראה שכבר ראית.** מסך הסקירה מציג עד 12 התראות (`over`/`risk`) מכל העסקאות,
  והן חוזרות בכל רינדור. ⚠️ הקושי אינו בכפתור אלא בזהות: ההתראות **מחושבות מחדש** ב-`buildAlerts`
  מהסנאפשוט ואין להן `id` — כדי לזכור "נסגרה" צריך **מפתח יציב** (למשל
  `dealId|teamId|סוג ההתראה`, ולא טקסט ההתראה, שמכיל מספרים משתנים).
  שתי החלטות שנדרשות: מתי התראה שנסגרה **חוזרת** (הצעה: כשהמצב מחמיר — למשל החריגה גדלה
  מעבר לסף — ולא רק מפני שהמספר זז), ואיפה נשמרת הסגירה (`settings`, כדי שתיכלל בגיבוי).
  שווה גם כפתור "הצג התראות שנסגרו", אחרת דבר שהוסתר נעלם לתמיד.
- **אין סינון עסקאות פעילות במסך הסקירה.** `renderOverview` מציג את כל העסקאות, כולל
  `closed`/`archived` — כך שגם ה-KPI המצטבר וההתראות כוללים עסקאות שהסתיימו.
  `DEAL_STATUSES` כבר קיים (`active` · `onhold` · `closed` · `archived`), חסר רק מסנן.
  להחליט: ברירת המחדל (הצעה: פעילות + בהמתנה), האם המסנן משפיע גם על **ה-KPI המצטבר ועל
  ההתראות** (הצעה: כן — אחרת המספרים לא תואמים את מה שרואים), והאם הבחירה נשמרת.
- **סדר אנשי הקשר לא נגזר מהתקציב.** היום `store.dealContacts` ממיין לפי שעות ואז לפי א״ב
  (`b.hours - a.hours || localeCompare`), והספרייה ממיינת א״ב בלבד. הסדר הנדרש:
  **לפי סדר הצוותים כמו במסך התקציב, ובתוך כל צוות לפי בכירות — שותף → עו״ד → מתמחה.**
  · סדר הצוותים = `team.order` (הסדר השמור, שנקבע בגרירה) ולא הקיבוץ הוויזואלי "דורש טיפול/בתוואי",
    שמשתנה לפי מצב הניצול ואינו סדר של המשתמש.
  · הבכירות אינה שדה: `DEFAULT_ROLES` מסודר **מהזוטר לבכיר** (מתמחה · עו״ד · שותף), ולכן הכלל
    הוא סדר הדרגות בתעריפון של העסקה **הפוך**. `junior` הוא דגל בלבד ולא דירוג, וכשמוסיפים דרגה
    ידנית היא נכנסת בסוף — כלומר הסדר בתעריפון הוא ההגדרה היחידה של בכירות, וכדאי שזה יהיה מודע.
  · אדם שמשויך לכמה צוותים באותה עסקה — למיין לפי הצוות הראשון שבו הוא מופיע.
  · חל על שלושת המקומות: חלון הבחירה לפני הייצוא, הגיליון המיוצא, ורשימת אנשי הקשר של העסקה.
    בספרייה הרוחבית אין הקשר של עסקה ולכן אין סדר צוותים — שם המיון יישאר א״ב
    (או לפי `defaultTeamName` ואז בכירות, אם יוחלט).
- **שפת השדות אינה אחידה בין המסכים.** בגיליון התקציב יש שלוש מחלקות מסומנות: שדה הזנה
  (`is-input` — נראה כשדה תמיד), ערך מחושב (`calcnum` — טקסט בלבד, לא ניתן להקלדה) וערך שנדרס
  (`is-override` — כתום עם ⚠ וכפתור חזרה לחישוב), יחד עם סימוני ƒ בכותרות ומקרא.
  ביתר המסכים: הטפסים (הגדרות, מודאלים) משתמשים ב-`.input`/`.select` — כלומר "שדה נראה כשדה" כבר
  מתקיים שם; טבלאות הדיווח והחשבונות הן קריאה בלבד ואין בהן קלט. מה שחסר הוא הצד השני של השפה —
  **סימון מה מחושב** ואזהרת דריסה — למשל מקדם החריגה בהגדרות שגוזר את שעות התקציב בלי שום סימון
  שהוא מזין חישוב במקום אחר. היקף התיקון קטן מכפי שנרשם קודם.
- **מחיקת עסקה ללא undo** — שחזור דורש גם את ה-blobs של המסמכים שנמחקו מ-`files`. מחיקת שורה/צוות
  כן קיבלו undo (טוסט 8 שניות; שחזור צוות מחזיר גם את ה-entries שעברו ל"ללא שיוך").
- `align-audit` מדלג על תאים שמכילים `input/select/button` — היישור שם נשען על ה-CSS בלבד ולא נבדק אוטומטית.
- אין ייצוא PDF בשום מסך (רק xlsx מעוצב + גיבוי JSON).

### 5א.3 סיווג צוות ופילוח לפי חבר צוות
- **`team.kind`** = `'dd' | 'regular'` (ברירת מחדל `regular`), נקבע בצ'קבוקס בפוטר גיליון הצוות.
  `model.aggregateByKind(snapshot)` מחזיר `{ hasDD, dd, regular }` מעל `aggregateTeams` הקיים,
  ו-`ui.ddSplitPanel(snap)` הוא אותו רכיב בשלושת המסכים (תקציב · בקרה · תחקיר) כדי שהחיתוך זהה בכולם.
  בייצואים: עמודת "סוג צוות" + שורות סיכום `כל צוותי בדיקת הנאותות` / `כל יתר הצוותים`.
- **"% מהצוות"** — `line.shareOfTeam` (שעות השורה מכלל שעות הצוות) ו-`people[].teamShares`
  (לכל אדם, שיעור לכל צוות שבו עבד). אדם בכמה צוותים מקבל שורה לכל צוות + שורת סיכום בייצוא.
- **שורות סיכום באקסל**: `rows` ב-`{ t:'table' }` יכולה להכיל `{ cells, kind:'subtotal' }` —
  רקע אפור בהיר, הדגשה וקו עליון; הסה"כ הכללי נשאר מובחן ממנה (רקע כהה יותר + קו כפול).
  כל סגנון חדש דורש עדכון של `count` ב-`fonts`/`fills`/`borders`/`cellXfs` ב-`STYLES_XML`,
  אחרת אקסל מסרב לפתוח את הקובץ.

### 5א.4 ספריית אנשי הצוות — מקור אמת יחיד לשמות  *(מיושם)*
**הכלל:** אין שם חופשי בשום מקום. שורת תקציב שומרת `personId` בלבד, ושם התצוגה נקרא תמיד
מהספרייה — לכן תיקון שם במסך המשותף משתקף מיד בכל העסקאות, המסכים והייצואים.

- **מודל**: store `people` (DB **v4**) — `{ id, key, name, title, defaultTeamName, email, phone,
  aliases[], active, notes }`. `key` = `importer.personKey` (מסיר תארים, הופך "משפחה, פרטי",
  מנקה פיסוק). `aliases` = כתיבים נוספים שנמצאו בדוחות — **נתוני זיהוי על אותה רשומה, לא מקור שני**.
- **`computeDeal` מקבל `people`** ומעשיר כל שורה ב-`personName`. כל צרכני התצוגה (תוויות שורה,
  תחקיר, ייצואים) קוראים `l.personName` — `l.person` נשאר ריק ואינו בשימוש אחרי המיגרציה.
  ⚠️ קריאה ל-`computeDeal` בלי `people` תציג שורות בלי שמות — יש ארבע קריאות, כולן מעבירות אותו.
- **מיגרציה** (`store.migratePeopleDirectory`, רצה בכל `loadAll`): כל `line.person` טקסטואלי הופך
  לרשומה לפי `personKey` (וריאציות כתיב מתאחדות + alias), השורה מקבלת `personId` והשדה מתרוקן.
  רץ גם על **גיבוי JSON ישן** שאין בו ספרייה — נבדק.
  `settings.peopleTeams` נקרא שם לתפקיד/צוות התחלתיים; מכאן `defaultTeamName` הוא המקור.
- **מסך "אנשי צוות"** (`state.view = 'people'`, כפתור ב-topbar ליד "תעריפונים"): עריכה בשורה,
  סימון פעיל/לא פעיל, הצעות מיזוג (`suggestPeopleMerges` מעל `fuzzyPersonMatch`), ייצוא/ייבוא/תבנית.
- **חלון הבחירה** (`ui.renderPersonPicker`): נפתח מתא "חבר צוות" בגיליון, מסנן לפי `personKey`
  (כך "כהן" מוצא "עו״ד דנה כהן"), וכשאין התאמה מציע **הקמה** עם השם שהוקלד. `pickerCtx` ב-app.js
  מחזיק את `{teamId, lineId}` הממתינים. גם "פרוס לפי אנשי צוות" עובד מול הספרייה (select), לא טקסט.
- **מחיקה**: `deletePerson` חוסמת כשיש שורות משויכות ומפנה לסימון "לא פעיל" — אחרת נוצר
  `personId` יתום. `mergePeople` מעביר את כל השורות לרשומה השורדת ושומר את השם השני כ-alias.
- **ייצוא אנשי קשר**: מהספרייה (לפי סימון) או מהגדרות העסקה — `store.dealContacts(dealId)` מזהה
  משויכים משלושה מקורות (שורת תקציב · אחראי צוות · דיווח שעות/חשבון) ומציין לכל אחד את **מקור השיוך**.
- **ייבוא אנשי קשר**: `importer.CONTACT_FIELDS` + `guessContactMapping` (משתמשים ב-`guessByFields`,
  אותה לוגיקת ניקוד של `guessMapping`) + `rowsToContacts`. לכל שורה סטטוס מול הספרייה —
  *חדש · קיים זהה · קיים עדכון · התאמה מקורבת* — עם החלטה לכל שורה ומתג "השלמת שדות ריקים בלבד"
  (ברירת מחדל) מול "עדכון מלא". `applyContactsImport` מחזיר עותק קודם ל**ביטול ייבוא** אחד.
  **הייבוא ממלא את הספרייה בלבד ואינו משייך אף אחד לעסקה** — שיוך הוא תמיד פעולה מפורשת בעסקה.
- **גיבוי**: `people` נכלל ב-`collectBackup`/`applyBackup` (לא קובץ נפרד).

### 5א.3 סיווג צוות ופילוח לפי חבר צוות
- **`team.kind`** = `'dd' | 'regular'` (ברירת מחדל `regular`), נקבע בצ'קבוקס בפוטר גיליון הצוות.
  `model.aggregateByKind(snapshot)` מחזיר `{ hasDD, dd, regular }` מעל `aggregateTeams` הקיים,
  ו-`ui.ddSplitPanel(snap)` הוא אותו רכיב בשלושת המסכים (תקציב · בקרה · תחקיר) כדי שהחיתוך זהה בכולם.
  בייצואים: עמודת "סוג צוות" + שורות סיכום `כל צוותי בדיקת הנאותות` / `כל יתר הצוותים`.
- **"% מהצוות"** — `line.shareOfTeam` (שעות השורה מכלל שעות הצוות) ו-`people[].teamShares`
  (לכל אדם, שיעור לכל צוות שבו עבד). אדם בכמה צוותים מקבל שורה לכל צוות + שורת סיכום בייצוא.
- **שורות סיכום באקסל**: `rows` ב-`{ t:'table' }` יכולה להכיל `{ cells, kind:'subtotal' }` —
  רקע אפור בהיר, הדגשה וקו עליון; הסה"כ הכללי נשאר מובחן ממנה (רקע כהה יותר + קו כפול).
  כל סגנון חדש דורש עדכון של `count` ב-`fonts`/`fills`/`borders`/`cellXfs` ב-`STYLES_XML`,
  אחרת אקסל מסרב לפתוח את הקובץ.

### 5א.4 בתכנון (לא מיושם) — ספריית אנשי צוות ואקסל אנשי קשר
**המטרה:** שם עורך דין מוקלד היום כטקסט חופשי בכל שורת תקציב ובכל ייבוא דוח. אין מקור אמת אחד,
אין דוא"ל/טלפון, וכל וריאציית כתיב יוצרת "אדם" נוסף. הרעיון: ספרייה רוחבית לכל העסקאות + שאיבה
ממנה בכל מקום שבו מזינים שם + ייצוא אנשי קשר לעסקה.

**מה כבר קיים ומשמש בסיס** (לא להתחיל מאפס):
`settings.peopleTeams` (זיכרון שם→צוות, לפי שם מנורמל), `store.knownPeople(dealId)` (איחוד אנשים
משורות התקציב, מהדיווחים ומהחשבונות), ו-`importer.personKey` / `fuzzyPersonMatch` שכבר מאחדים
תארים, סדר הפוך וראשי תיבות. חסרים: רשומה אמיתית עם מזהה, שדות קשר, וממשק.

1. **מודל — מקור אמת יחיד.** store חדש `people` (DB v4):
   `{ id, key (personKey), name, title, defaultTeamName, email, phone, aliases[], active, notes }`.
   ⚠️ **שורת תקציב שומרת `personId` בלבד — לא טקסט.** שם התצוגה נקרא תמיד מהספרייה, ולכן שינוי
   שם במסך המשותף משתקף מיד בכל העסקאות. אין "שם חופשי" בשום מקום, וגם לא כגיבוי.
   `aliases` אינו מקור נוסף אלא נתוני זיהוי על אותה רשומה (כתיב שנמצא בדוח שיובא).
   מיגרציה v3→v4: כל `person` קיים הופך לרשומה בספרייה לפי `personKey` (שמות שנבדלים רק בכתיב
   מתאחדים לרשומה אחת + alias), השורות מקבלות `personId`, והשדה הישן נמחק אחרי ההמרה.
   **`peopleTeams` מתקפל לתוך `people.defaultTeamName`** — לקרוא מהמפתח הישן עוד גרסה אחת לצורך
   המיגרציה בלבד.
2. **מסך "אנשי צוות"** — אח של "תעריפונים" ב-topbar (`state.view = 'people'`), חוצה עסקאות:
   שם · תפקיד · צוות · דוא"ל · טלפון · בכמה עסקאות · שעות שדווחו · פעולות. עריכה בשורה בשפת
   השדות של הגיליון. **מיזוג כפילויות** (רשומה שורדת + העברת aliases) — זו הפעולה הקריטית, כי
   הספרייה תתמלא משמות שיובאו; ההצעות למיזוג נשענות על `fuzzyPersonMatch` הקיים.
3. **בחירה בעסקה = חלון בורר, לא הקלדה חופשית.** בכל מקום שמזינים בו שם היום (תא "חבר צוות"
   בגיליון, שיוך אנשים בייבוא דוח שעות, "פרוס לפי אנשי צוות") נפתח **חלון בחירה** מהספרייה:
   רשימת האנשים, שדה חיפוש שמסנן תוך כדי הקלדה (לפי `personKey` — כך "כהן" מוצא גם
   "עו"ד דנה כהן"), ובחירה מחזירה `personId`. כשאין התאמה, החלון מציע **"הקם איש צוות חדש"**
   עם השם שהוקלד כבר בשדה — הרשומה נשמרת למסך המשותף ורק אז נבחרת. אין מסלול שבו נשמר שם
   שלא קיים בספרייה.
   *(לא `<datalist>`: הוא לא מאפשר להציג תפקיד/צוות לצד השם, ולא לתפוס את המקרה "לא נמצא —
   להקים חדש" כפעולה מפורשת.)*
   שם בדוח שיובא שלא נמצא בספרייה: או הקמה מהחלון, או שהשעות נשארות **"ללא שיוך"** (מושג
   שכבר קיים במנוע) — לא נוצרת רשומת צל.
4. **אקסל אנשי קשר לעסקה** — מחולל אחד, שתי כניסות: (א) בחירת שורות במסך אנשי הצוות;
   (ב) כפתור בהגדרות העסקה שמזהה לבד מי משויך — שורת תקציב בעסקה · דיווח שעות בעסקה ·
   `team.lead` — ומסמן לכל אחד **את מקור השיוך**, עם רשימת סימון לפני הייצוא.
   גיליון "אנשי קשר": שם · תפקיד · צוות · דוא"ל · טלפון · מקור השיוך · שעות בעסקה · % מהצוות,
   עם שורות סיכום לפי צוות (`kind:'subtotal'` הקיים).
5. **ייבוא אנשי קשר מאקסל** (למסך המשותף בלבד — לא לעסקה). אותו דפוס כמו שאר הייבואים במערכת,
   ועל אותה תשתית: `xlsx.readTabularFile` + `pickBestSheet` + `detectHeaderRow` + `guessMapping`
   (עם מילון נרדפות לאנשי קשר: שם/שם מלא/name · תפקיד/דרגה/title/role · צוות/מחלקה/team ·
   דוא"ל/אימייל/mail/email · טלפון/נייד/phone/mobile), ומסך תצוגה מקדימה עם בוררי עמודות
   כמו ב-`renderProgressImportPreview`.
   **סטטוס לכל שורה מול הספרייה** (זו עיקר העבודה, לא הפרסור):
   · *חדש* — תיווצר רשומה · *קיים, זהה* — דילוג · *קיים, עדכון* — הצגת ישן→חדש, עם מתג
   "השלמת שדות ריקים בלבד" (ברירת מחדל) מול "עדכון מלא" · *התאמה מקורבת* דרך `fuzzyPersonMatch` —
   "האם זה אותו אדם?" לאישור, ואישור מוסיף את הכתיב שבקובץ כ-`alias` · *כפילות בתוך הקובץ עצמו* —
   שתי שורות עם אותו `personKey`, לבחור אחת.
   שורה בלי שם — מדולגת עם ספירה. אימייל/טלפון: בדיקת תקינות בסיסית בלבד, בלי נרמול אגרסיבי
   שישבור מספרים בפורמט בינלאומי.
   **גבול חשוב:** הייבוא ממלא את הספרייה בלבד ואינו משייך אף אחד לעסקה — שיוך נשאר פעולה מפורשת
   בעסקה, אחרת קובץ אקסל אקראי "מצרף" אנשים לעסקאות.
   **ביטול ייבוא** — לשמור עותק של ה-store לפני ההחלה ולאפשר "בטל ייבוא" אחד (טוסט הביטול הקיים);
   ייבוא של 200 שורות הוא בדיוק המקרה שבו טעות אחת יקרה.
   **תבנית להורדה** — כפתור "הורד תבנית אקסל" שמפיק קובץ עם העמודות המדויקות דרך `xlsx-write`;
   חוסך את רוב עבודת המיפוי מלכתחילה.
6. **נגזרות של "מקור אמת יחיד"**:
   · שני אנשים באותו שם — אפשרי, כי המפתח הוא `personId`; הספרייה תתריע על שם כפול ותאפשר להבחין
     ביניהם (תפקיד/צוות בשורה), אבל לא תמנע.
   · **מחיקת אדם שמשויך לעסקאות** חייבת להתנהג כמו מחיקת צוות: לחסום או להעביר את השורות
     ל"ללא שיוך" עם ביטול — אחרת נשארות שורות עם `personId` יתום.
   · שינוי שם בספרייה משנה תצוגה **רטרואקטיבית בכל העסקאות והייצואים** — זו התנהגות רצויה כאן,
     אבל צריך שתהיה מודעת (ייצוא ישן שנשמר בדיסק לא ישתנה, כמובן).
7. **החלטות פתוחות**: פרטיות (דוא"ל/טלפון נכנסים ל-IndexedDB ולגיבוי ה-JSON) · האם הספרייה
   נשמרת בגיבוי המאוחד או בקובץ נפרד (היא חוצה עסקאות, כמו התעריפונים).

### 5א.2 סבב עיצוב מסך התקציב (היררכיית בולטוּת)
- **הבעיה שאובחנה**: ~120 מספרים באותו משקל, 5 כרטיסי צוות זהים, 3,163px גלילה. המספר הגדול
  והזהוב ביותר היה "תקציב" (נתון קבוע), בעוד התחזית לסיום והצוות שחרג ב-111% היו חסרי בולטוּת.
- **הפס העליון** = שלוש שאלות בשלושה גדלים: ניצול (2.6rem) · יתרה · תחזית לסיום (EAC, לא הופיעה קודם
  בטאב התקציב), לצד **מפת התקציב** — רוחב מקטע = חלקו של הצוות, לחיצה פותחת וממקדת.
  פאנל "סיכום תקציב העסקה" **הוסר** — הוא חזר על אותם מספרים.
- **צוותים**: כרטיס → שורה ברשימה, קיבוץ "דורש טיפול"/"בתוואי", רק חורג נפתח אוטומטית. גלילה → ~2,000px.
  מצב הפתיחה נזכר לכל עסקה (`lb_open_<dealId>`).
- **הגיליון בשלוש רמות**: עוגן (שעות בוצע/תקציב + מד שאורכו יחסי לגודל השורה) ← תוצר מחושב (₪) ←
  קלט תכנון. עמודת "חריגה" ריקה כשאין חריגה — דיו רק איפה שיש בעיה; פס חומרה בקצה השורה במקום צביעת שורה.
- **שדה מחושב אינו ניתן להקלדה** (שעות תקציב, תעריף). דריסה = פעולה מפורשת (כפתור) → שדה כתום עם ⚠
  וכפתור חזרה לחישוב, כדי שלא יישאר ערך "תקוע" בלי לדעת. מקרא מתחת לגיליון.
- **גרירה**: מנוע אחד לצוותים (אנכי), טאבי עסקאות (אופקי) ושורות דרגה, עם ידית, קו יעד ו-Alt+חצים.
  התנגשות עם המיון לפי דחיפות נפתרה במתג "לפי דחיפות / הסדר שלי" — גרירה מקבעת את הסדר הנראה.

## 6. לקחים אחרונים
- **SW cache-first שבר פרסומים** (HTML התעדכן, JS ישן הוגש) → מעבר ל-network-first + auto-reload. בכל שינוי app-shell: להעלות `CACHE`.
- מטמון SW תקוע אצל משתמש דורש איפוס חד-פעמי (Clear site data) לפני שהלוגיקה החדשה תופסת.
- `origin_mismatch` ב-OAuth = ה-origin לא רשום (lironcon.com מול github.io) — להוסיף את שניהם.
- בכרטיסים: שם/תפקיד היו inline ונדבקו → `card__id`/`pcard__id` הפכו `flex-direction:column`.
- drawer: כפתור `×` התנגש עם מחיקה → `padding-top` ל-`.drawer__body .detail`.
- **grid blowout במובייל** (רשימה 460px על viewport 390): טראק `1fr` לא מוגבל ל-min-content → `grid-template-columns: minmax(0, 1fr)` + `min-width: 0` ל-sidebar/main.
- header גלש במסכים ≤520px → breakpoint קומפקטי (root בלבד; ל-v2 יש sidebar).
- **כרטיסים נדחסו ונחתכו ברשימה מלאה**: `.list`/`.lane__cards` הם flex-column גלילים, ו-`overflow:hidden` על `.card`/`.pcard` מאפס את ה-min-height האוטומטי של flex item → הכרטיסים כווצו במקום שהרשימה תגלול. תוקן ב-`flex-shrink: 0` (v1+v2). לקח: באג שתלוי בכמות נתונים — לשחזר עם רשימה שגולשת מגובה החלון, לא עם 4 רשומות דמו.
- **חותמת גרסה**: `budget/tools/stamp.mjs` כותב את `budget/js/version.js` (תאריך, commit, גרסת SW,
  ערוץ web/offline) והוא מוצג במסך ההגדרות. נולד מהשאלה "איך אני יודע איזה קובץ מעודכן" — בלי חותמת
  שני בילדים נראים זהים. הסקריפט רץ אוטומטית מתוך `build-offline.mjs`, ולכן החותמת תמיד מתעדכנת.
- **מיזוג ענף העיצוב (`budget-screen-design`) לתוך האלוקציה**: ספריית האנשים הפכה למקור האמת לשמות —
  שורת תקציב מחזיקה `personId` (ולא טקסט), וב-snapshot השם מגיע כ-`personName`. כל קוד שקורא שם
  מהשורה חייב `personName`/`store.personNameOf(personId)`; `l.person` נשאר ריק אחרי המיגרציה.
  `addTeamMember` (הקמת איש צוות מדוח שעות) מזהה אדם קיים לפי מפתח/alias, שומר את הכתיב שבדוח
  כ-alias, ורק אם אין התאמה יוצר רשומה חדשה בספרייה.
- **`VersionError: the requested version (3) is less than the existing version (4)`** — קובץ אופליין/ענף
  אחר יצר את `lexBudgetDB` בגרסה גבוהה יותר, ו-IndexedDB **אוסר הורדת גרסה**, ולכן הבילד הישן
  נחסם לגמרי. תוקן ב-`budget/js/db.js`: `openDB` תופס `VersionError`, נפתח **בגרסה הקיימת**
  (`indexedDB.open` בלי מספר), ורק אם חסר store נדרש מעלה גרסה בשלב אחד. בנוסף `DB_VERSION`
  יושר ל-4 ונוצר store `people` כדי שהסכימה תהיה זהה בין ענפים. **לקח:** מספר גרסת DB הוא
  מרחב גלובלי חוצה-ענפים — אף פעם לא להוריד אותו, ולא להניח שהמסד במכשיר נוצר בקוד הזה.
- **`normalizeProgress` מסנן שדות לא מוכרים**: `fileId` הוצמד לרשומות הייבוא ב-app.js אבל נמחק בנרמול,
  ולכן הקישור לקובץ במסך "מקורות המידע" לא עבד. השדה נוסף לנרמול. לקח: כל שדה חדש ברשומה חייב
  להופיע ב-`normalize*` המתאים, אחרת הוא נעלם בשקט בשמירה.
- **ספירה כפולה בתיבת השעות (LexBudget)**: התיבה בגיליון *הציגה* את הסך המאוחד מכל המקורות אבל
  *כתבה* דרך `setLineManualTotal` את הסך **הידני** — ולכן הקלדת סך על שורה שכבר היה בה דוח התווספה
  אליו (21 מדוח + 33 שהוקלדו = 54 → ניצול 180%). בנוסף `refreshComputed` מעולם לא סנכרן את התיבה
  בחזרה, ולכן אחרי מחיקה היא נשארה ריקה בזמן שהשורה הציגה מספר אחר. לקח: **תיבה שמציגה ערך מאוחד
  חייבת גם לקבוע אותו** (החלק הידני = הפרש מול המקורות הוודאיים), וכל שדה שמוצג ממודל חייב להתיישר
  בחזרה בכל רענון — אחרת המסך והמודל מתפצלים בלי שאף אחד ישים לב.
- **v2 ≤900px: ה-nav drawer היה תקוע באמצע המסך** — RTL: ‏`inset-inline-end`=left, ולכן `translateX(100%)` דחף לאמצע. תוקן ל-`inset-inline-start` (=ימין). בנוסף: ה-backdrop ישב על `body::after` מחוץ ל-stacking context של `.app` (z-index:1) וכיסה גם את המגירה — הועבר ל-`.app::after` (‏z-39, מגירה z-40) + קליק עליו סוגר (`refs.app` ב-app.js).
